[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smart_plotters"
version = "0.0.1"

dependencies = [
 "numpy",
 "astropy",
 "regions",
 "reproject",
 "spectral_cube",
 "matplotlib"
]
